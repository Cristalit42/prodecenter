{
  "devDependencies": {
    "@parcel/transformer-sass": "^2.13.0",
    "parcel": "^2.13.0",
    "posthtml-include": "^2.0.1",
    "sass": "^1.81.0",
    "swiper": "^11.1.15"
  },
  "scripts": {
    "dev": "parcel serve --no-cache ./src/index.html --open",
    "build": "parcel build ./src/index.html --no-content-hash --no-source-maps --no-cache --no-optimize --dist-dir build --public-url ./"
  }
}
